// Constantes partagées client + serveur : rien ici ne doit importer de code
// serveur-only (base de données, next/server) pour rester safe côté client.

/** Score minimum (sur 100) pour débloquer le starter kit. "7/10" à l'oral. */
export const KIT_SCORE_THRESHOLD = 70;
